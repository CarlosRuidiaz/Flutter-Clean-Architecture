/// ESQUELETO. Lo completa la PAREJA PROFILE (persona 2), al final.
///
/// Mismo papel que project_dependencies.dart: es el composition root de
/// este feature y solo se puede cerrar cuando las tres capas existan.
void registerProfile() {
  // TODO(profile persona 2): armar la cadena.
  //   Get.put<IProfileSource>(LocalProfileSource());
  //   Get.put<IProfileRepository>(ProfileRepository(Get.find()));
  //   Get.lazyPut(() => ProfileController(Get.find()));
}
